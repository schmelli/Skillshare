---
phase: 01-foundation-access-control
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/api/package.json
  - apps/api/prisma/schema.prisma
  - apps/api/prisma.config.ts
  - apps/api/prisma/seed.ts
  - apps/api/src/prisma/prisma.service.ts
  - apps/api/src/prisma/prisma.module.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/health/health.module.ts
  - apps/api/src/app.module.ts
  - apps/api/test/schema-tenant-scoping.spec.ts
  - apps/api/test/health.e2e-spec.ts
  - apps/web/src/routes/index.tsx
  - apps/web/src/lib/api.ts
  - .env
autonomous: true
requirements: [DEPL-03]
user_setup: []

must_haves:
  truths:
    - "The Prisma schema uses the Prisma 7 `prisma-client` generator with an explicit `output` path (NOT `prisma-client-js`), and `prisma.config.ts` wires the `@prisma/adapter-pg` driver adapter with `DATABASE_URL`."
    - "The data model is organization-scoped from day one: `Workspace.tenantId` is a NON-NULL foreign key to `Tenant`, and `Membership` carries both a `userId` and a `workspaceId` (DEPL-03)."
    - "`npx prisma migrate dev --name init` creates the initial migration and applies every model (Tenant, Workspace, Membership, and the better-auth core tables) to the live Postgres 18 database."
    - "The bootstrap seed writes exactly one `Tenant` row (real DB write), and re-running the seed does not create a second Tenant (idempotent upsert)."
    - "`GET /api/health` performs a real `prisma.tenant.count()` read and the React index route renders the returned count, proving the SPA → API → Prisma → Postgres path works end to end (real DB read + real UI interaction)."
    - "A structural test asserts via the Prisma DMMF that every tenant-scoped model carries a `tenantId` or `workspaceId` foreign field, so a future multi-tenant mode needs no schema redesign (DEPL-03)."
    - { statement: "Edge probe returned 'unclassified' for DEPL-03 — manually reviewed at plan time: the salient concern is that every tenant-scoped entity carries a non-null tenant/workspace FK from day one so no backfill migration is ever needed. Resolved as: Workspace.tenantId is NON-NULL with a Tenant FK and one Tenant is seeded at bootstrap. Recorded as an explicit assumption, not silently dropped.", verification: backstop }
  artifacts:
    - path: apps/api/prisma/schema.prisma
      provides: "Tenant, Workspace, Membership models + better-auth core tables"
      contains: "model Tenant"
    - path: apps/api/prisma.config.ts
      provides: "Prisma 7 datasource + @prisma/adapter-pg driver adapter config"
      contains: "adapter"
    - path: apps/api/src/prisma/prisma.service.ts
      provides: "Injectable PrismaClient wrapper (connect on module init)"
    - path: apps/api/prisma/seed.ts
      provides: "Bootstrap seed of one Tenant (idempotent)"
    - path: apps/api/src/health/health.controller.ts
      provides: "GET /api/health with real prisma.tenant.count() read"
      exports: ["health"]
  key_links:
    - from: apps/web/src/routes/index.tsx
      to: apps/api/src/health/health.controller.ts
      via: "fetch GET /api/health via TanStack Query, renders tenant count"
      pattern: "api/health"
    - from: apps/api/src/health/health.controller.ts
      to: apps/api/prisma/schema.prisma
      via: "prisma.tenant.count()"
      pattern: "tenant\\.count"
    - from: apps/api/prisma.config.ts
      to: docker-compose.yml
      via: "DATABASE_URL points at the compose Postgres service"
      pattern: "DATABASE_URL"
  prohibitions: []
---

<objective>
Stand up the Prisma 7 data layer against Postgres 18 with the organization-scoped
`Tenant → Workspace → Membership` schema (DEPL-03), run the initial migration, seed one Tenant,
and complete the Walking Skeleton's thinnest end-to-end proof: a health route that does a real
`prisma.tenant.count()` read, rendered by the React dashboard.

Purpose: Prove the full stack (SPA → NestJS/Fastify → Prisma 7 driver-adapter → Postgres 18) is
wired correctly and the future-proof org-scoped model exists from the first migration, before any
auth/workspace feature logic depends on it.
Output: A live database with the org-scoped schema applied, an idempotent Tenant seed, and a
dashboard landing page rendering live DB data.
</objective>

<execution_context>
@/home/user/Skillshare/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Skillshare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-access-control/01-RESEARCH.md
@.planning/phases/01-foundation-access-control/SKELETON.md
@apps/api/src/app.module.ts
@apps/api/src/app.controller.ts
@apps/web/src/routes/index.tsx
</context>

<assumption_delta_decision>
**Noun under review:** Workspace (multi-workspace / "another workspace" pluralization signal from
the phase requirement text "a role in one workspace confers no rights in another").
**Decision:** no-change.
**Rationale:** The org-scoped `Tenant → Workspace → Membership` identity model is the PRIMARY data
model established here from day one (that is exactly what DEPL-03 + the ORG requirements mandate).
"Another workspace" is the inherent multi-workspace design, not a newly-introduced second identity
noun requiring promotion or an add-alongside migration. No accepted debt.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prisma 7 org-scoped schema + config + PrismaService + seed + failing structural/health tests</name>
  <files>apps/api/package.json, apps/api/prisma/schema.prisma, apps/api/prisma.config.ts, apps/api/prisma/seed.ts, apps/api/src/prisma/prisma.service.ts, apps/api/src/prisma/prisma.module.ts, apps/api/src/app.module.ts, apps/api/test/schema-tenant-scoping.spec.ts, apps/api/test/health.e2e-spec.ts, .env</files>
  <read_first>
    - .planning/phases/01-foundation-access-control/01-RESEARCH.md (Pitfall 1 Prisma 7 breaking changes; Pattern 3 Tenant parent table; Code Examples: better-auth Prisma adapter; Anti-Patterns: prisma-client-js / missing output / skipping driver adapter)
    - apps/api/src/app.module.ts (current module wiring from Plan 01)
    - .env.example (DATABASE_URL shape from Plan 01)
    - .planning/phases/01-foundation-access-control/01-VALIDATION.md (schema-tenant-scoping test intent)
  </read_first>
  <behavior>
    - schema-tenant-scoping.spec: introspect the Prisma DMMF; assert `Workspace` has a `tenantId` scalar field that is required (NOT nullable) and a `tenant` relation to `Tenant`; assert `Membership` has required `userId` and `workspaceId` fields; FAILS until the schema exists.
    - health.e2e-spec: boot the Nest app, `GET /api/health` returns 200 with `{ status: 'ok', tenantCount: <number> }`; FAILS until the health route reads from Prisma.
  </behavior>
  <action>
    Add Prisma deps to `apps/api/package.json`: `prisma @prisma/client @prisma/adapter-pg pg` (all
    7.8.x except `pg` 8.22.x) and dev dep `tsx` for the seed. Write `apps/api/prisma/schema.prisma`
    with `generator client { provider = "prisma-client"  output = "../src/generated/prisma" }` (Prisma
    7 syntax — NOT `prisma-client-js`, and the explicit `output` is mandatory) and
    `datasource db { provider = "postgresql" }` (URL lives in prisma.config.ts, not here). Define:
    `model Tenant { id String @id @default(cuid()); name String; createdAt DateTime @default(now());
    workspaces Workspace[] }`; `model Workspace { id String @id @default(cuid()); tenantId String
    (NON-NULL); tenant Tenant @relation(fields:[tenantId], references:[id]); name String; createdAt
    DateTime @default(now()); memberships Membership[]; @@unique([tenantId, name]) }`; `model
    Membership { id String @id @default(cuid()); userId String; workspaceId String; role WorkspaceRole;
    createdAt DateTime @default(now()); @@unique([userId, workspaceId]) }`; and `enum WorkspaceRole
    { admin editor consumer }`. Also declare the better-auth core tables (`User`, `Session`, `Account`,
    `Verification`, and organization-plugin tables `Organization`, `Member`, `Invitation`) — mirror
    the fields better-auth's generated Prisma fragment expects; `User.email` MUST carry `@unique`.
    Write `apps/api/prisma.config.ts` (Prisma 7): import `defineConfig`, construct `new PrismaPg({
    connectionString: process.env.DATABASE_URL })`, export the schema path + migrations config + the
    adapter (per RESEARCH Code Examples — the adapter is mandatory in Prisma 7, no Rust engine).
    Write `apps/api/src/prisma/prisma.service.ts` (Injectable, extends the generated PrismaClient,
    instantiated with the PrismaPg adapter, `connect()` on `onModuleInit`) and `prisma.module.ts`
    (global module exporting PrismaService). Write `apps/api/prisma/seed.ts` that upserts one Tenant
    (idempotent — a fixed `id`/name, `upsert` not `create`, so re-running is safe). Create `.env` from
    `.env.example` with a real `BETTER_AUTH_SECRET` value. Add the two failing tests described in
    <behavior>. Register `PrismaModule` in `app.module.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @skillshare/api exec prisma generate && pnpm --filter @skillshare/api vitest run test/schema-tenant-scoping.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` contains `provider = "prisma-client"` and `output =` and does NOT contain `prisma-client-js`
    - `schema.prisma` contains `model Tenant`, `model Workspace`, `model Membership`, `enum WorkspaceRole`, `@@unique([tenantId, name])`, and `@@unique([userId, workspaceId])`
    - `Workspace.tenantId` is required (no `?` nullable marker)
    - `apps/api/prisma.config.ts` contains `PrismaPg` and `DATABASE_URL`
    - `prisma generate` exits 0 producing the client under `src/generated/prisma`
    - `schema-tenant-scoping.spec.ts` runs (RED before Task 2 wiring is complete for health; the DMMF schema assertions pass once the schema exists)
  </acceptance_criteria>
  <done>Prisma 7 schema + driver-adapter config generate a client; the org-scoped structural test asserts the tenant/workspace FKs; the health e2e is written and RED.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Apply initial migration + wire real health read + dashboard render (GREEN)</name>
  <files>apps/api/prisma/migrations/, apps/api/src/health/health.controller.ts, apps/api/src/health/health.module.ts, apps/api/src/app.module.ts, apps/web/src/lib/api.ts, apps/web/src/routes/index.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-access-control/01-RESEARCH.md (Pitfall 1; Environment Availability — Docker/Postgres present)
    - apps/api/prisma/schema.prisma (schema from Task 1)
    - apps/api/src/prisma/prisma.service.ts (injectable client from Task 1)
    - apps/web/src/routes/index.tsx (placeholder landing from Plan 01)
    - apps/web/src/main.tsx (QueryClientProvider wiring from Plan 01)
  </read_first>
  <action>
    [BLOCKING — schema push] Ensure `docker compose up -d postgres` is healthy, then run
    `npx prisma migrate dev --name init` from `apps/api` (non-interactive on a fresh DB; this creates
    `prisma/migrations/*_init` and applies it). If the environment blocks `migrate dev`'s shadow
    database, fall back to `npx prisma db push` and record the deviation in the SUMMARY. Then run the
    seed: `pnpm --filter @skillshare/api exec tsx prisma/seed.ts`.
    Build the health slice: write `apps/api/src/health/health.controller.ts` exposing
    `GET /api/health` that injects `PrismaService` and returns `{ status: 'ok', tenantCount: await
    prisma.tenant.count() }` (real DB read), plus `health.module.ts`; register it in `app.module.ts`
    and REMOVE the static health route from `AppController` (single source of truth). On the web side,
    write `apps/web/src/lib/api.ts` (a typed `fetch` helper honoring `credentials: 'include'` and the
    API base URL) and update `apps/web/src/routes/index.tsx` to fetch `/api/health` via TanStack Query
    and render "API connected — {tenantCount} tenant(s)" (real UI interaction wired to the API).
  </action>
  <verify>
    <automated>pnpm --filter @skillshare/api vitest run test/schema-tenant-scoping.spec.ts test/health.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/migrations/` contains an `*_init` migration directory
    - `GET /api/health` returns `{ status: 'ok', tenantCount: 1 }` after seeding (curl or e2e)
    - `apps/api/src/health/health.controller.ts` contains `tenant.count`
    - `apps/web/src/routes/index.tsx` fetches `api/health` and renders the tenant count
    - `schema-tenant-scoping.spec.ts` and `health.e2e-spec.ts` both exit 0 (GREEN)
  </acceptance_criteria>
  <done>The initial migration is applied to Postgres, one Tenant is seeded, and the dashboard renders a live DB read — the Walking Skeleton is proven end to end.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols/files (excluded from drift verification as newly-minted):
- Prisma models: `Tenant`, `Workspace`, `Membership`, `enum WorkspaceRole`, better-auth core tables (`User`, `Session`, `Account`, `Verification`, `Organization`, `Member`, `Invitation`)
- Files: `apps/api/prisma/schema.prisma`, `apps/api/prisma.config.ts`, `apps/api/prisma/seed.ts`, `apps/api/src/generated/prisma/*` (generated), initial migration under `apps/api/prisma/migrations/`
- Classes: `PrismaService`, `PrismaModule`, `HealthController`, `HealthModule`
- Web: `apps/web/src/lib/api.ts` fetch helper, updated `index.tsx` health route
- Tests: `schema-tenant-scoping.spec.ts`, `health.e2e-spec.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Postgres | All DB access flows through Prisma parameterized queries |
| browser → API | Health read crosses the SPA→API boundary (dev cross-origin) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Prisma DB queries | high | mitigate | Exclusively Prisma parameterized queries via the driver adapter; no raw SQL string concatenation anywhere (RESEARCH Security Domain — SQL injection). Canon breadcrumb: SQL injection is covered by /gsd-secure-phase + eslint; not minted as a bespoke prohibition. |
| T-02-02 | Information Disclosure | `.env` `BETTER_AUTH_SECRET` | high | mitigate | `.env` is git-ignored (Plan 01); secret is generated locally, never committed |
| T-02-03 | Elevation of Privilege | future multi-tenant data isolation | low | accept | Postgres RLS is a v2/hardening addition per CLAUDE.md; v1 self-hosted is single-tenant. The `tenantId` FK exists now so RLS can be layered later without a schema redesign (DEPL-03). |
| T-02-SC | Tampering | Prisma/pg installs | high | mitigate | Covered by Plan 01 Task 1 legitimacy checkpoint; exact versions pinned |
</threat_model>

<verification>
- `prisma generate` produces the client at the configured `output` path.
- `prisma migrate dev --name init` applies the schema to Postgres 18.
- The Tenant seed is idempotent (re-run leaves exactly one Tenant).
- `GET /api/health` returns a live `tenantCount`; the dashboard renders it.
- The DMMF structural test confirms org-scoped FKs on every tenant-scoped model.
</verification>

<success_criteria>
The org-scoped schema is live in Postgres, the seed writes one Tenant, and the dashboard renders a
real DB read — the thinnest end-to-end slice of the Walking Skeleton works and DEPL-03 is satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-access-control/01-02-SUMMARY.md` when done.
</output>

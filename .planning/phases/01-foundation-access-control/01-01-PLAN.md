---
phase: 01-foundation-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - turbo.json
  - package.json
  - tsconfig.base.json
  - eslint.config.mjs
  - .prettierrc.json
  - docker-compose.yml
  - .env.example
  - .gitignore
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/nest-cli.json
  - apps/api/vitest.config.ts
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/app.controller.ts
  - apps/api/test/setup.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/vite.config.ts
  - apps/web/vitest.config.ts
  - apps/web/index.html
  - apps/web/components.json
  - apps/web/src/main.tsx
  - apps/web/src/routes/__root.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/index.css
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
autonomous: false
requirements: [DEPL-03]
user_setup: []

must_haves:
  truths:
    - "Running `pnpm install` at the repo root resolves all workspace packages with zero errors."
    - "`pnpm turbo build` builds apps/api, apps/web, and packages/shared green."
    - "`pnpm turbo test` runs the Vitest harness in apps/api and apps/web and exits 0 (empty/placeholder suites pass)."
    - "`docker compose up -d postgres` starts a Postgres 18 container that reports healthy via its healthcheck."
    - "The NestJS API boots on port 3000 with the Fastify adapter and `bodyParser: false`, and `GET /api/health` returns a 200 JSON body."
    - "The React 19 + Vite dev server boots on 5173 and renders the root route without console errors."
    - "The deployment substrate for DEPL-03 exists from day one: a single `docker compose up` brings up the datastore with no external dependencies to install first."
  artifacts:
    - path: pnpm-workspace.yaml
      provides: "Workspace globs apps/* and packages/*"
      contains: "packages/*"
    - path: turbo.json
      provides: "Turborepo build/test/lint task pipeline"
      contains: "tasks"
    - path: docker-compose.yml
      provides: "Postgres 18 service with healthcheck and named volume"
      contains: "postgres:18"
    - path: apps/api/src/main.ts
      provides: "NestJS Fastify bootstrap with bodyParser:false"
      contains: "bodyParser"
    - path: apps/api/vitest.config.ts
      provides: "Wave 0 API test harness"
    - path: apps/web/vitest.config.ts
      provides: "Wave 0 web test harness"
    - path: apps/web/components.json
      provides: "shadcn/ui new-york/neutral preset applied"
  key_links:
    - from: apps/web/vite.config.ts
      to: apps/api/src/app.controller.ts
      via: "Vite dev proxy / fetch to http://localhost:3000/api during dev"
      pattern: "3000|/api"
    - from: docker-compose.yml
      to: .env.example
      via: "DATABASE_URL points at the compose Postgres service"
      pattern: "DATABASE_URL"
  prohibitions: []
---

<objective>
Scaffold the greenfield Skillshare monorepo: pnpm workspaces + Turborepo, a NestJS 11 (Fastify)
API skeleton, a React 19 + Vite dashboard skeleton with shadcn/ui, a shared package, the Vitest
test harness (Wave 0), and a Docker Compose Postgres 18 service. This is the ground the Walking
Skeleton stands on — no feature logic yet, but every tool boots and the test harness is green.

Purpose: Establish the locked stack exactly once so subsequent slices (auth, workspaces, roles)
build against a working toolchain instead of re-deriving scaffolding. Satisfies the DEPL-03
deployment substrate (single `docker compose up`, no external services).
Output: A committable monorepo where `pnpm install`, `pnpm turbo build`, `pnpm turbo test`, and
`docker compose up -d postgres` all succeed.
</objective>

<execution_context>
@/home/user/Skillshare/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Skillshare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-access-control/01-RESEARCH.md
@.planning/phases/01-foundation-access-control/01-UI-SPEC.md
@.planning/phases/01-foundation-access-control/SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (pre-install gate)</name>
  <what-built>
    Nothing built yet — this gate runs BEFORE the first `pnpm add`. RESEARCH.md's Package
    Legitimacy Audit tags `@prisma/adapter-pg` and `pg` as [ASSUMED] (package names discovered
    via WebSearch; Context7 unavailable that session), while approving all other locked-stack
    packages. Per the phase supply-chain gate (threat T-01-SC), the two [ASSUMED] packages must be
    human-verified against the registry before installation.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@prisma/adapter-pg — confirm it is published by the
       Prisma org (repository github.com/prisma/prisma) and version 7.8.x exists.
    2. Open https://www.npmjs.com/package/pg — confirm it is `node-postgres` (repository
       github.com/brianc/node-postgres), the de-facto standard Postgres driver, version 8.22.x.
    3. Confirm neither page shows a typosquat name, a suspicious recent-only publish history, or a
       postinstall script that is not part of the documented package.
  </how-to-verify>
  <action>
    Pause before the first `pnpm add`. Present the two [ASSUMED]-tagged packages
    (`@prisma/adapter-pg` 7.8.x, `pg` 8.22.x) and the verification steps above to the developer.
    Do NOT run any install until the developer confirms. This gate is never auto-approvable.
  </action>
  <resume-signal>Type "approved" to proceed with installs, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Monorepo toolchain + Docker Compose Postgres + shared package</name>
  <files>pnpm-workspace.yaml, turbo.json, package.json, tsconfig.base.json, eslint.config.mjs, .prettierrc.json, docker-compose.yml, .env.example, .gitignore, packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts</files>
  <read_first>
    - .claude/CLAUDE.md (locked stack table + versions; What NOT to Use)
    - .planning/phases/01-foundation-access-control/01-RESEARCH.md (Standard Stack versions, Recommended Project Structure, Environment Availability)
    - .gitignore (current repo ignore rules)
  </read_first>
  <action>
    Create the pnpm + Turborepo monorepo root. Write `pnpm-workspace.yaml` with globs `apps/*` and
    `packages/*` (do NOT create empty `apps/cli`, `apps/mcp-server`, or `packages/skill-parser` —
    deferred to Phases 2/5 per RESEARCH). Write root `package.json` with `"packageManager":
    "pnpm@10.33.0"`, `"engines": { "node": ">=24" }`, and scripts `build`/`test`/`lint`/`dev`
    delegating to `turbo`. Write `turbo.json` with a `tasks` pipeline for `build`, `test`, `lint`
    (build `dependsOn: ["^build"]`). Write `tsconfig.base.json` (TypeScript 5.9, `strict: true`,
    `moduleResolution: "bundler"`), a root flat `eslint.config.mjs`, and `.prettierrc.json`.
    Write `docker-compose.yml` with a single `postgres` service on image `postgres:18`, a named
    volume `pgdata`, env `POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB=skillshare`, port `5432:5432`,
    and a `healthcheck` using `pg_isready`. Do NOT add Redis, S3/MinIO, or any other service
    (CLAUDE.md constraint: app + postgres only). Write `.env.example` with `DATABASE_URL=postgresql://skillshare:skillshare@localhost:5432/skillshare`,
    `BETTER_AUTH_SECRET=`, `BETTER_AUTH_URL=http://localhost:3000`, `WEB_ORIGIN=http://localhost:5173`.
    Extend `.gitignore` for `node_modules`, `dist`, `.turbo`, `.env`, and Prisma generated output.
    Create `packages/shared` with its own `package.json` (name `@skillshare/shared`, type module),
    `tsconfig.json` extending the base, and `src/index.ts` exporting a placeholder `export const SKILLSHARE_SHARED = true;`.
    Run `pnpm add -Dw turbo typescript prettier eslint` at the root and `pnpm install`.
  </action>
  <verify>
    <automated>pnpm install && docker compose up -d postgres && sleep 3 && docker compose ps --format '{{.Health}}' | grep -qi healthy</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contains `apps/*` and `packages/*`
    - root `package.json` contains `"node": ">=24"` and `"packageManager": "pnpm@`
    - `docker-compose.yml` contains `postgres:18` and a `healthcheck:` key and does NOT contain `redis`, `minio`, or `s3`
    - `.env.example` contains `DATABASE_URL=postgresql://`
    - `pnpm install` exits 0 and `docker compose ps` shows the postgres service `healthy`
  </acceptance_criteria>
  <done>Root monorepo installs cleanly, Postgres 18 container is healthy, shared package builds.</done>
</task>

<task type="auto">
  <name>Task 3: NestJS+Fastify API skeleton + React+Vite dashboard skeleton + Vitest harness (Wave 0)</name>
  <files>apps/api/package.json, apps/api/tsconfig.json, apps/api/nest-cli.json, apps/api/vitest.config.ts, apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/src/app.controller.ts, apps/api/test/setup.ts, apps/web/package.json, apps/web/tsconfig.json, apps/web/vite.config.ts, apps/web/vitest.config.ts, apps/web/index.html, apps/web/components.json, apps/web/src/main.tsx, apps/web/src/routes/__root.tsx, apps/web/src/routes/index.tsx, apps/web/src/index.css</files>
  <read_first>
    - .planning/phases/01-foundation-access-control/01-RESEARCH.md (Pattern 1 main.ts bootstrap with bodyParser:false; Recommended Project Structure; Validation Architecture / Wave 0 Gaps)
    - .planning/phases/01-foundation-access-control/01-UI-SPEC.md (Design System preset: shadcn new-york / neutral / cssVariables; Inter font)
    - tsconfig.base.json (base compiler options created in Task 2)
    - turbo.json (task pipeline created in Task 2)
  </read_first>
  <action>
    Scaffold `apps/api` (NestJS 11 + Fastify): `package.json` (name `@skillshare/api`) with deps
    `@nestjs/core @nestjs/common @nestjs/platform-fastify @nestjs/config` and dev deps `vitest
    @nestjs/testing supertest`. Write `src/main.ts` using `NestFactory.create<NestFastifyApplication>(AppModule, new FastifyAdapter(), { bodyParser: false })`
    listening on port 3000 (host 0.0.0.0), with CORS enabled for `process.env.WEB_ORIGIN`. Write
    `src/app.module.ts` importing `ConfigModule.forRoot({ isGlobal: true })` and declaring
    `AppController`. Write `src/app.controller.ts` exposing `GET /api/health` returning
    `{ status: 'ok' }` (a real DB-backed health read is added in Plan 02 — keep it static here).
    Add `apps/api/vitest.config.ts` and `apps/api/test/setup.ts` as the Wave 0 API harness with one
    trivial passing smoke test so `pnpm turbo test` is green.
    Scaffold `apps/web` (React 19 + Vite 7): `package.json` (name `@skillshare/web`) with deps
    `react react-dom @tanstack/react-router @tanstack/react-query` and dev deps `vite @vitejs/plugin-react
    @tanstack/router-plugin tailwindcss @tailwindcss/vite vitest @testing-library/react jsdom`.
    Write `vite.config.ts` (react + tanstackRouter + tailwind plugins; dev server port 5173), a
    minimal `index.html`, `src/main.tsx` (mounts the TanStack Router + QueryClientProvider),
    `src/routes/__root.tsx` (root layout, Inter font stack), and `src/routes/index.tsx` (renders a
    placeholder "Skillshare" heading — replaced by the health page in Plan 02). Run `npx shadcn@latest
    init` non-interactively with the UI-SPEC preset (style=new-york, baseColor=neutral,
    cssVariables=true, Tailwind v4), producing `components.json` and `src/index.css`. Add
    `apps/web/vitest.config.ts` with one trivial passing smoke test. Do NOT install MDXEditor, diff
    viewers, or any Phase 2+ UI libraries.
  </action>
  <verify>
    <automated>pnpm turbo build && pnpm turbo test && pnpm --filter @skillshare/api exec node -e "require('fs').accessSync('src/main.ts')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/main.ts` contains `bodyParser` and `FastifyAdapter`
    - `apps/api/src/app.controller.ts` contains a `health` route returning `status`
    - `apps/web/components.json` exists and contains `"style": "new-york"` and `"baseColor": "neutral"`
    - `apps/web/src/routes/__root.tsx` and `apps/web/src/routes/index.tsx` exist
    - `apps/api/vitest.config.ts` and `apps/web/vitest.config.ts` exist (Wave 0 harness)
    - `pnpm turbo build` exits 0 and `pnpm turbo test` exits 0
  </acceptance_criteria>
  <done>API and dashboard skeletons boot, shadcn is initialized to the UI-SPEC preset, the Wave 0 Vitest harness is green across both packages.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files/symbols created here (excluded from drift verification as newly-minted):
- Config: `pnpm-workspace.yaml`, `turbo.json`, root `package.json`, `tsconfig.base.json`, `eslint.config.mjs`, `.prettierrc.json`, `docker-compose.yml`, `.env.example`
- API: `apps/api/` package, `AppModule`, `AppController`, `GET /api/health` (static), `main.ts` Fastify bootstrap
- Web: `apps/web/` package, `components.json` (shadcn new-york/neutral), `__root.tsx`, `index.tsx` routes, `src/index.css`
- Shared: `@skillshare/shared` package, `src/index.ts`
- Test harness: `apps/api/vitest.config.ts`, `apps/api/test/setup.ts`, `apps/web/vitest.config.ts` (Wave 0)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → npm registry | Package installs pull third-party code that runs at install/build time |
| browser → API (dev) | Cross-origin 5173 → 3000 during local dev |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (`@prisma/adapter-pg`, `pg`, all locked-stack deps) | high | mitigate | Task 1 blocking-human legitimacy checkpoint for the two [ASSUMED] packages; all other packages Approved in RESEARCH Package Legitimacy Audit; `[SUS]` `@thallesp/nestjs-better-auth` explicitly NOT installed; pin exact versions |
| T-01-01 | Information Disclosure | CORS config in `main.ts` | medium | mitigate | CORS restricted to `WEB_ORIGIN` env allowlist, not `*`; credentials-aware origin echo only for the trusted dev origin |
| T-01-02 | Spoofing | `.env` secrets committed | high | mitigate | `.env` in `.gitignore`; only `.env.example` (no real secrets) is committed; `BETTER_AUTH_SECRET` left blank in the example |
</threat_model>

<verification>
- `pnpm install` resolves the workspace with zero errors.
- `pnpm turbo build` and `pnpm turbo test` both exit 0.
- `docker compose up -d postgres` reports the service healthy.
- `.env` is git-ignored; only `.env.example` is tracked.
</verification>

<success_criteria>
The monorepo boots end to end at the toolchain level: install, build, test, and the Postgres
container all succeed. No feature logic exists yet — this is the scaffold for the Walking Skeleton.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-access-control/01-01-SUMMARY.md` when done.
</output>

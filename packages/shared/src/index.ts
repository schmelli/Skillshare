// Placeholder export for the shared package. Real Zod schemas and DTO/permission
// types (skill schema, workspace/role types) are added in Phase 2+ as the domain
// model grows. This keeps `@skillshare/shared` a resolvable workspace dependency
// from Wave 0 onward.
export const SKILLSHARE_SHARED = true;
